---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: githubrepositories.controllers.trishanku.org.trishanku.org
spec:
  group: controllers.trishanku.org.trishanku.org
  names:
    kind: GitHubRepository
    listKind: GitHubRepositoryList
    plural: githubrepositories
    singular: githubrepository
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GitHubRepository is the Schema for the githubrepositories API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GitHubRepositorySpec defines the desired state of GitHubRepository.
            properties:
              credentialsSecretName:
                description: CredentialsSecretName specifies the secret containing
                  the GitHub credentials.
                type: string
              credentialsSecretTokenKey:
                description: CredentialsSecretTokenKey specifies key name to look
                  up the credentials token from the credentials secret.
                type: string
              description:
                description: Description for the GitHub repository.
                type: string
              organization:
                description: Organization for the GitHub repository.
                type: string
              private:
                description: Private specifies if the GitHub repository should be
                  private.
                type: boolean
            type: object
          status:
            description: GitHubRepositoryStatus defines the observed state of GitHubRepository
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
